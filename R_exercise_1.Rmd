---
title: "R的练习之一"
date: '`r strftime(Sys.time(), format = "%B %d, %Y")`'
output:
  html_document: default
---


#### 作业要求


* 完成此作业需要你使用rmarkdown，并编译生成html文件，html文件请用自己的学号命名（仅需要学号即可，不用写姓名）。
* 此作业所需的数据为**CEPS基线调查学生数据.sav**，问卷是**"九年级学生问卷.pdf"**和**"七年级学生问卷.pdf""**，数据和问卷都在微云的共享文件夹里，微信群里发给大家。也可以点击如下[***链接***](http://url.cn/46mFZvi) 获取。
* 请按照下述要求完成作业，请将所用的r代码写在下面这样的代码块里，对rmd不熟悉的同学可参看本课程的第一次课件**“R_markdown.Rmd”**。
* 作业请上传至学校[***FTP***](Ftp://210.35.207.34)，账号是*1200600734work*，密码是*123456*，登录后请进入文件夹*“/R/R练习一”*，并将作业上传至此。


---


##### 0. 首先，你得在R里把**CEPS基线调查学生数据.sav**读到R里来。

```{r}

```

---

##### 1. 请从数据中任选5-8个变量，并报告其数据缺失值的情况。
```{r}

```

---

##### 2. 请任选若干个变量，并**按行**计算其和、均值和标准差，当然前提是**这几个变量具有可加性**，比如都是对时间进行测量的变量。
```{r}

```

---

##### 3. 请任选一个numeric类型的变量，对其进行recode，将其转化为factor类型的变量后定义其levels，当然，你还得报告下各个水平的频数和百分比。
```{r}

```


---

##### 4. 请从数据中选择一个子集，并从中挑选1个变量，报告其最小值、Q1、Q2、均值、Q3和最大值。
```{r}

```

---

##### 5. 请从数据中选择一个变量，对其进行统计作图（包括boxplot，histogram，还需要在histogram上加上正态分布曲线和核密度函数曲线），当然前提是**这个变量可以画这样的图**。
```{r}

```

---

##### 6. 从数据中选择两个变量，假设A和B，以A为分组变量，计算在A的不同水平下变量B的均值和标准差，并画出可以对比的boxplot。
```{r}

```

---

##### 7. 请从数据中选择一个factor类型的变量，对其做柱状图。
```{r}

```













